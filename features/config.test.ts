const config = {
	search: {
		dropdownFilter: false,
	},
	insightChat: {
		hideSidebar: true,
		hideNewChatHeader: true,
		allowChangeModel: false,
	},
	vote: {
		legacyStyle: false,
	},
}

export default config
